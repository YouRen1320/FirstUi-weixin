<view class="fui-drag__item-outer" style="width:{{width}}px;height:{{height}}px">
  <view class="fui-drag__item-wrap fui-drag__item-padding" style="width:{{width-(padding*2)}}px;height:{{height-(padding*2)}}px;background:{{background}};justify-content:space-between;">
    <view class="fui-drag__item-inner">
      <view class="fui-drag__item-icon fui-drag__item-mr" catchtap="handleDel" wx:if="{{isDel}}">
        <fui-icon name="minussign-fill" color="{{delColor}}" size="{{48}}"></fui-icon>
      </view>
      <text class="fui-drag__item-text" style="color:{{color}};font-size:{{size}}rpx" wx:if="{{text!='' && text!==true}}">{{model.text}}</text>
    </view>
    <fui-icon name="menu" size="{{48}}" color="{{slideColor}}" wx:if="{{isMenu}}"></fui-icon>
  </view>
</view>