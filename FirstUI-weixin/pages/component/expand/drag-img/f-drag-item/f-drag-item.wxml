<view class="fui-drag__item-outer" style="width:{{width}}px;height:{{height}}px">
		<view class="fui-drag__item-wrap {{type==2?'fui-drag__item-padding':''}}" 
		style="width:{{width-(padding*2)}}px;height:{{height-(padding*2)}}px;background:{{background}};justify-content:center;">
			<view class="fui-drag__item-inner fui-drag__item-column">
				<image style="width:{{width-(padding*2)}}px;height:{{height-(padding*2)}}px" src="{{model.src}}" class="fui-drag__item-img" wx:if="{{model.src && model.src!==true && model.src!=='true'}}" >
				</image>
				<view class="fui-drag__item-icon fui-drag__item-ab" catchtap="handleDel" wx:if="{{isDel}}">
					<fui-icon name="clear-fill" color="{{delColor}}" size="{{48}}"></fui-icon>
				</view>
			</view>
		</view>
	</view>