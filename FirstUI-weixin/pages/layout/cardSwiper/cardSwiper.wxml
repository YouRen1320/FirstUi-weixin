<view class="fui-wrap">
    <view class="fui-page__hd">
			<view class="fui-page__title fui-align__center" bindtap="vip">CardSwiper <image
					src="/static/images/index/light/icon_member_3x.png"></image>
			</view>
			<view class="fui-page__desc">卡片轮播，常用的页面布局，可提升开发效率以及带来舒服的视觉效果！</view>
		</view>
		<view class="fui-page__bd">
			<view class="fui-section__title">卡片轮播</view>
			<!-- 布局内容 start -->
			<fui-swiper-dot items="{{items}}" current="{{current}}">
				<swiper previous-margin="60rpx" next-margin="60rpx" class="fui-card__swiper" bindchange="change" circular
					indicator-dots="{{false}}" autoplay interval="{{5000}}" duration="{{150}}">
					<swiper-item wx:for="{{items}}" wx:key="index" class="fui-cs__swiper-item">
						<view class="fui-cardswiper__item {{current!==index?'fui-cardswiper__scale':''}}">
							<image src="{{item.src}}" class="fui-cardswiper__img"></image>
						</view>
					</swiper-item>
				</swiper>
			</fui-swiper-dot>

			<view class="fui-section__title">卡片轮播</view>
			<fui-swiper-dot items="{{items}}" current="{{current2}}" styles="{{styles}}">
				<view class="fui-cardswiper__box">
					<swiper class="fui-card__swiper-wrap" bindchange="change2" circular indicator-dots="{{false}}" autoplay
						interval="{{5000}}" duration="{{150}}">
            <swiper-item wx:for="{{items}}" wx:key="index" class="fui-cs__swiper-item">
							<view class="fui-cardswiper__item">
								<image src="{{item.src}}" class="fui-cardswiper__img"></image>
							</view>
						</swiper-item>
					</swiper>
				</view>
			</fui-swiper-dot>

			<view class="fui-section__title">卡片轮播</view>
			<view class="fui-cardswiper__box">
				<swiper class="fui-card__swiper-wrap" circular indicator-dots="{{false}}" autoplay vertical
					interval="{{5000}}" duration="{{150}}">
					<swiper-item wx:for="{{items}}" wx:key="index" class="fui-cs__swiper-item">
						<view class="fui-cardswiper__item">
							<image src="{{item.src}}" class="fui-cardswiper__img"></image>
						</view>
					</swiper-item>
				</swiper>
			</view>
			<!-- 布局内容 end -->
		</view>
		<view class="fui-btn__box">
      <fui-button type="gray" width="400rpx" height="84rpx" text="More" bold margin="24rpx" bindclick="href">
			</fui-button>
		</view>
	</view>