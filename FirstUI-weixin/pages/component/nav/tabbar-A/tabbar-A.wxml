<view class="fui-wrap">
  <view class="fui-page__hd">
    <view class="fui-page__title fui-align__center" bindtap="vip">Tabbar <image src="/static/images/index/light/icon_member_3x.png"></image>
    </view>
    <view class="fui-page__desc">Tabbar 标签栏，用于在不同功能模块之间进行切换，小图标建议尺寸84*84。</view>
  </view>
  <view class="fui-page__bd">
    <view class="fui-section__title">基础使用</view>
    <fui-tabbar isFixed="{{false}}" tabBar="{{tabBar2}}" current="{{current}}" bindclick="itemClick"></fui-tabbar>
    <view class="fui-section__title">纯文本</view>
    <fui-tabbar isFixed="{{false}}" background="rgba(0,0,0,.5)" color="rgba(255,255,255,.7)" selectedColor="#fff" size="32" font-weight="500" tabBar="{{tabBar}}" current="{{current}}" bindclick="itemClick"></fui-tabbar>
    <view class="fui-section__title">纯图标</view>
    <fui-tabbar isFixed="{{false}}" tabBar="{{tabBar3}}" current="{{current}}" bindclick="itemClick"></fui-tabbar>
    <view class="fui-section__title">中间使用图标</view>
    <fui-tabbar isFixed="{{false}}" tabBar="{{tabBar4}}" current="{{current2}}" bindclick="itemClick2"></fui-tabbar>
    <view class="fui-section__title">中间大图标（凸出）</view>
    <fui-tabbar isFixed="{{false}}" tabBar="{{tabBar5}}" current="{{current2}}" bindclick="itemClick2"></fui-tabbar>
    <view class="fui-section__title">带角标</view>
    <fui-tabbar isFixed="{{false}}" tabBar="{{tabBar6}}" current="{{current}}" bindclick="itemClick"></fui-tabbar>
    <view class="fui-section__title">使用字体图标（fui-icon）</view>
    <fui-tabbar isFixed="{{false}}" selected-color="#FFB703" tabBar="{{tabBar7}}" current="{{current}}" bindclick="itemClick"></fui-tabbar>
  </view>
</view>